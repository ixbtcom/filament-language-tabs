# Финальное решение проблемы с EditorJS в LanguageTabs

> **Дата**: 2025-10-09
> **Статус**: Готово к реализации
> **Файл для изменения**: `/Users/mikhailpanyushkin/code/xcom/filament-editorjs/resources/js/editorjs.js`

---

## Краткое описание проблемы

При переключении табов с локалями EditorJS показывает **пустой редактор** вместо содержимого:

- Открываешь `ru` → контент есть ✅
- Переключаешься на `en` → пустой редактор ❌
- Если `en` первый в конфиге → работает ✅

**Корневая причина:**

EditorJS создается один раз в `init()` и НЕ обновляется при изменении Alpine `state` через `$entangle`. При переключении табов Livewire обновляет state, но EditorJS остается "замороженным" на старых данных.

---

## ⚠️ ВАЖНО: Последовательность действий

**Применяй варианты по порядку:**

1. **Вариант 1** (простой) → тестируй → если работает, **СТОП**
2. Если Вариант 1 не помог → **Вариант 2** (продвинутый) → тестируй → если работает, **СТОП**
3. Если Вариант 2 не помог → **Вариант 3** (полный пересоздание)

**НЕ применяй все варианты сразу!** Каждый следующий вариант нужен только если предыдущий не сработал.

---

## Вариант 1: Простой watcher (НАЧНИ С ЭТОГО)

### Описание

Добавляем простой `$watch` на изменение state, который вызывает `render()` для обновления EditorJS.

### Что добавить

**Файл**: `/Users/mikhailpanyushkin/code/xcom/filament-editorjs/resources/js/editorjs.js`

**Где**: После строки 490 (после блока `onReady`)

**Найди:**
```js
onReady: () => {
    const editor = this.instance;
    new Undo({ editor });
    new DragDrop(editor);
},
```

**Добавь СРАЗУ ПОСЛЕ:**
```js
});  // Конец new EditorJS({...})

// ========================================
// ВАРИАНТ 1: Простой watcher для обновления EditorJS
// ========================================
this.$watch('state', async (newState, oldState) => {
    // Пропускаем если данные не изменились
    if (JSON.stringify(newState) === JSON.stringify(oldState)) {
        this.log('State unchanged, skipping update');
        return;
    }

    this.log('State changed, updating EditorJS', { newState, oldState });

    // Ждем готовности EditorJS
    if (!this.instance || !this.instance.isReady) {
        this.log('EditorJS not ready');
        return;
    }

    try {
        // Обновляем данные через render()
        await this.instance.render(newState || { blocks: [] });
        this.log('EditorJS updated successfully');
    } catch (error) {
        console.error('EditorJS render failed:', error);
    }
});
```

### Пересборка

```bash
cd /Users/mikhailpanyushkin/code/xcom/filament-editorjs
npm run build
```

### Тестирование

1. Перезагрузи страницу с формой
2. Открой DevTools → Console
3. Переключи табы `ru → en → ru`
4. Должны видеть логи: `State changed, updating EditorJS`
5. Контент должен обновляться

### ✅ Если работает → СТОП, больше ничего не нужно!

---

## Вариант 2: Watcher с нормализацией и защитой от циклов (ЕСЛИ ВАРИАНТ 1 НЕ ПОМОГ)

### Описание

Более продвинутый watcher с:
- Нормализацией данных (EditorJS ожидает `{blocks: [...]}`)
- Защитой от циклических обновлений через флаг
- Правильной обработкой пустых локалей через `clear()`

### Что изменить

**ВНИМАНИЕ:** Если применял Вариант 1, **УДАЛИ** его код перед применением Варианта 2.

**Файл**: `/Users/mikhailpanyushkin/code/xcom/filament-editorjs/resources/js/editorjs.js`

#### Шаг 1: Добавить флаг в return объект

**Найди строку 32:**
```js
return {
    instance: null,
    state,
    statePath,
    // ...
```

**Добавь после `state`:**
```js
return {
    instance: null,
    state,
    isUpdatingFromEditor: false,  // ← ДОБАВИТЬ
    statePath,
    // ...
```

#### Шаг 2: Добавить метод нормализации

**В конце return объекта (перед закрывающей `};`), после метода `log()` (строка ~50):**

```js
log(...args) {
    if (this.debugEnabled) {
        console.log(...args);
    }
},

// ========================================
// ВАРИАНТ 2: Нормализация данных EditorJS
// ========================================
normalizeEditorData(value) {
    if (!value || typeof value !== 'object') {
        return { blocks: [] };
    }

    // Если уже есть blocks - используем как есть
    if (Array.isArray(value.blocks)) {
        return {
            ...value,
            blocks: value.blocks.filter(Boolean),
        };
    }

    // Если это массив - считаем что это blocks
    if (Array.isArray(value)) {
        return {
            blocks: value.filter(Boolean),
            version: '2.0',
        };
    }

    return { blocks: [] };
},
```

#### Шаг 3: Обновить onChange

**Найди строку ~480:**
```js
onChange: () => {
    this.instance.save().then((outputData) => {
        this.state = outputData;
    });
},
```

**Замени на:**
```js
onChange: async () => {
    // Флаг предотвращает рекурсивный вызов watcher
    this.isUpdatingFromEditor = true;

    try {
        const outputData = await this.instance.save();
        this.state = this.normalizeEditorData(outputData);
    } catch (error) {
        console.error('EditorJS save failed:', error);
    }

    // Сбрасываем флаг в следующем тике
    this.$nextTick(() => {
        this.isUpdatingFromEditor = false;
    });
},
```

#### Шаг 4: Добавить watcher

**После строки 490 (после `onReady`):**

```js
});  // Конец new EditorJS({...})

// ========================================
// ВАРИАНТ 2: Продвинутый watcher с нормализацией
// ========================================
this.$watch('state', async (newValue, oldValue) => {
    // Игнорируем собственные изменения
    if (this.isUpdatingFromEditor) {
        this.log('Update from editor, skipping watcher');
        return;
    }

    // Нормализуем данные
    const data = this.normalizeEditorData(newValue);
    const prev = this.normalizeEditorData(oldValue);

    // Проверяем реальное изменение
    if (JSON.stringify(data) === JSON.stringify(prev)) {
        this.log('Normalized data unchanged');
        return;
    }

    this.log('State changed, updating EditorJS', { data, prev });

    // Ждем готовности
    await this.instance?.isReady;

    if (!this.instance) {
        this.log('No EditorJS instance');
        return;
    }

    try {
        // Если данных нет - очищаем редактор
        if (!data.blocks?.length) {
            await this.instance.clear();
            this.log('EditorJS cleared');
            return;
        }

        // Иначе рендерим новые данные
        await this.instance.render(data);
        this.log('EditorJS rendered with', data.blocks.length, 'blocks');
    } catch (error) {
        console.error('EditorJS update failed:', error);
    }
});
```

### Пересборка

```bash
cd /Users/mikhailpanyushkin/code/xcom/filament-editorjs
npm run build
```

### Тестирование

1. Перезагрузи страницу
2. Открой DevTools → Console
3. Отредактируй текст в `ru` → должен появиться лог `Update from editor, skipping watcher`
4. Переключи на `en` → должен появиться лог `State changed, updating EditorJS`
5. Переключи на пустую локаль → должен появиться лог `EditorJS cleared`
6. Все должно работать без ошибок

### ✅ Если работает → СТОП, Вариант 3 не нужен!

---

## Вариант 3: Полное пересоздание EditorJS (ТОЛЬКО ЕСЛИ ВАРИАНТЫ 1 И 2 НЕ ПОМОГЛИ)

### Описание

Радикальный подход: полностью уничтожаем и пересоздаем EditorJS instance при каждом изменении state.

### ⚠️ Недостатки

- Медленнее (~100-300ms на создание)
- Видимое "мерцание" при переключении
- Теряется undo/redo история
- Больше нагрузка на память

### Что изменить

**ВНИМАНИЕ:** Если применял Вариант 1 или 2, **УДАЛИ** их код перед применением Варианта 3.

**Файл**: `/Users/mikhailpanyushkin/code/xcom/filament-editorjs/resources/js/editorjs.js`

#### Шаг 1: Вынести создание EditorJS в отдельный метод

**В return объекте, после метода `log()` (строка ~50):**

```js
log(...args) {
    if (this.debugEnabled) {
        console.log(...args);
    }
},

// ========================================
// ВАРИАНТ 3: Метод создания/пересоздания EditorJS
// ========================================
async createEditor(initialData) {
    // Уничтожаем старый instance если есть
    if (this.instance) {
        try {
            await this.instance.isReady;
            this.instance.destroy();
            this.log('Previous EditorJS instance destroyed');
        } catch (error) {
            console.error('Failed to destroy EditorJS:', error);
        }
        this.instance = null;
    }

    const toolsOptions = this.toolsOptions ?? {};
    const tools = this.tools ?? [];
    const uploadImageEndpoint = this.uploadImageEndpoint;
    const uploadImageByUrlEndpoint = this.uploadImageByUrlEndpoint;
    const fetchUrlEndpoint = this.fetchUrlEndpoint;
    const minHeight = this.minHeight ?? 0;
    const placeholder = this.placeholder ?? '';
    const readOnly = this.readOnly ?? false;
    const csrfToken = document.querySelector('meta[name="csrf-token"]')?.getAttribute('content') ?? '';

    // ... ВСЯ ЛОГИКА НАСТРОЙКИ enabledTools из init() ...
    // (код строки 53-373 переносится сюда)

    const enabledTools = {};
    // ... копируешь весь код настройки tools из init() ...

    // Создаем новый EditorJS
    this.instance = new EditorJS({
        holder: this.$el,
        minHeight,
        data: initialData || { blocks: [] },
        placeholder,
        readOnly,
        tools: enabledTools,
        i18n: {
            // ... вся i18n конфигурация ...
        },
        onChange: async () => {
            const outputData = await this.instance.save();
            this.state = outputData;
        },
        onReady: () => {
            const editor = this.instance;
            new Undo({ editor });
            new DragDrop(editor);
        },
    });

    this.log('EditorJS created with', initialData?.blocks?.length || 0, 'blocks');
},
```

#### Шаг 2: Изменить init()

**Замени весь метод init() на:**

```js
async init() {
    // Создаем начальный editor
    await this.createEditor(this.state);

    // Watcher для пересоздания при изменении state
    this.$watch('state', async (newState, oldState) => {
        if (JSON.stringify(newState) === JSON.stringify(oldState)) {
            this.log('State unchanged');
            return;
        }

        this.log('State changed, recreating EditorJS', { newState, oldState });

        // Добавляем небольшую задержку для плавности
        await new Promise(resolve => setTimeout(resolve, 50));

        await this.createEditor(newState);
    });
},
```

### ⚠️ ВНИМАНИЕ

Этот вариант требует **большой рефакторинг** метода `init()`. Он самый сложный в реализации, но гарантирует работу в любых условиях.

### Пересборка

```bash
cd /Users/mikhailpanyushkin/code/xcom/filament-editorjs
npm run build
```

### Тестирование

1. Перезагрузи страницу
2. Переключение табов будет медленнее (видно мерцание)
3. Но контент должен всегда отображаться корректно
4. Проверь что редактирование работает

---

## Сравнение вариантов

| Критерий | Вариант 1 | Вариант 2 | Вариант 3 |
|----------|-----------|-----------|-----------|
| **Сложность** | ✅ Очень простой | 🟡 Средняя | 🔴 Высокая |
| **Время реализации** | 5 минут | 15-20 минут | 1-2 часа |
| **Скорость работы** | ✅ Быстро | ✅ Быстро | 🔴 Медленно |
| **Надежность** | 🟡 Может не работать в edge cases | ✅ Высокая | ✅ Гарантированно |
| **Сохранение undo** | ✅ Да | ✅ Да | ❌ Нет |
| **Визуальные эффекты** | ✅ Нет | ✅ Нет | 🔴 Мерцание |
| **Когда использовать** | Попробовать первым | Если Вариант 1 не помог | Последний вариант |

---

## Рекомендуемый порядок действий

### Шаг 1: Применить Вариант 1

1. Открыть `/Users/mikhailpanyushkin/code/xcom/filament-editorjs/resources/js/editorjs.js`
2. Найти строку 490 (после `onReady`)
3. Добавить код из Варианта 1
4. Пересобрать: `npm run build`
5. Тестировать

### Шаг 2: Если Вариант 1 не помог

1. **Удалить код Варианта 1**
2. Применить Вариант 2 (все 4 шага)
3. Пересобрать: `npm run build`
4. Тестировать

### Шаг 3: Если Вариант 2 не помог

1. **Удалить код Варианта 2**
2. Применить Вариант 3 (большой рефакторинг)
3. Пересобрать: `npm run build`
4. Тестировать

---

## Контрольный список тестирования

После применения любого варианта проверь:

- [ ] Переключение `ru → en` → контент обновляется
- [ ] Переключение `en → ru` → контент возвращается
- [ ] Переключение на пустую локаль → показывается пустой редактор
- [ ] Редактирование в `ru` → изменения сохраняются
- [ ] Переключение на `en` и обратно → изменения в `ru` остались
- [ ] Сохранение формы → данные в БД корректны
- [ ] Нет ошибок в DevTools Console
- [ ] Множественные EditorJS на странице работают независимо

---

## Откат изменений

Если что-то пошло не так:

```bash
cd /Users/mikhailpanyushkin/code/xcom/filament-editorjs
git checkout resources/js/editorjs.js
npm run build
```

---

## Примечания

### Debug режим

Если нужно видеть логи работы EditorJS:

```php
// В форме
EditorJs::make('content')
    ->debug(true)  // ← Включить debug
```

### Проверка текущего состояния

В DevTools Console:

```js
// Найти EditorJS instance
const editorEl = document.querySelector('[x-data*="editorjs"]');
const alpine = Alpine.$data(editorEl);

console.log('Current state:', alpine.state);
console.log('EditorJS ready:', alpine.instance?.isReady);
console.log('Blocks count:', alpine.instance?.blocks?.getBlocksCount());
```

### Если проблема осталась

Проверь:

1. EditorJS правильно подключен? `x-load` работает?
2. Нет ли конфликтов версий Alpine/Livewire?
3. `$entangle` работает? (проверь другие поля)
4. Builder правильно обрабатывается в LanguageTabs?
5. Данные в БД корректны? (JSON структура)

---

## Заключение

**Начни с Варианта 1** - он самый простой и в большинстве случаев решает проблему.

**Переходи к Варианту 2** только если Вариант 1 не помог.

**Используй Вариант 3** в крайнем случае - он гарантированно работает, но имеет недостатки.

**НЕ применяй все варианты сразу!** Тестируй по порядку.

Удачи! 🚀
